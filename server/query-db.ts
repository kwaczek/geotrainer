import mongoose from 'mongoose'; import QuizResult from './src/models/QuizResult'; async function main() { try { await mongoose.connect('mongodb://localhost:27017/geotrainer'); console.log('Connected to MongoDB'); const count = await QuizResult.countDocuments(); console.log('Total quiz results:', count); if (count > 0) { const results = await QuizResult.find().limit(5).lean(); console.log('Sample quiz results:', JSON.stringify(results, null, 2)); } mongoose.disconnect(); } catch (err) { console.error('Error:', err); } } main();
