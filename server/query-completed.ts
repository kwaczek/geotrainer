import mongoose from 'mongoose'; import QuizResult from './src/models/QuizResult'; async function main() { try { await mongoose.connect('mongodb://localhost:27017/geotrainer'); console.log('Connected to MongoDB'); const completedCount = await QuizResult.countDocuments({isCompleted: true}); console.log('Completed quiz results:', completedCount); if (completedCount > 0) { const results = await QuizResult.find({isCompleted: true}).limit(3).lean(); results.forEach((result, i) => { console.log(); console.log(); console.log(); console.log(); console.log(); console.log(); console.log(); console.log(); console.log(); console.log('- Question sample:'); if (result.questionAttempts.length > 0) { const sample = result.questionAttempts[0]; console.log(); console.log(); console.log(); } }); } mongoose.disconnect(); } catch (err) { console.error('Error:', err); } } main();
